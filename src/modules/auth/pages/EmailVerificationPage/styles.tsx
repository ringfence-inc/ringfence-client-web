import styled, { css } from "styled-components";

// Components
import _EmailVerification, {
  EmailVerificationProps,
} from "../../components/EmailVerification";

// Styled components
export const EmailVerification = styled(
  _EmailVerification
)<EmailVerificationProps>``;
